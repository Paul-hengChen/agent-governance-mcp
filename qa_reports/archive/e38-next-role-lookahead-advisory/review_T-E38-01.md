# QA review — T-E38-01

<!-- Auto-appended by tw_update_state(qa_review=...). -->

## 2026-07-27T04:20:19.372Z — PASS — by qa-engineer

PASS. New file test/e38-next-role-lookahead.test.mjs (no existing file — e28-shrink-warning.test.mjs, e35-pipeline-order.test.mjs, qa-flow.test.mjs — covers next_role-lookahead scope; created new file mirroring e28's through-the-tool pattern, flagged for override in the final report). 10 tests through the tw_update_state tool boundary (handleUpdateState envelope), not the bare helper, per the note that a probe against dist logic is not a committed pin. Covers: (1) live shape qa-engineer:FAIL+next_role=design-auditor warns naming pm:In_Progress/sr-engineer:In_Progress; (2) E37 edge (qa-engineer:PASS+next_role=design-auditor) silent at hop 2 AND hop 10 — hop 10 is the round-1 regression pin; (3) all three whitelists silent (resume_of, round-cap collapse, self-loop); (4) non-empty NAMED remedy on qa-engineer:In_Progress and pm:Blocked, asserting the real successors via the "pair(s): ..." segment, not just non-empty — the round-2 regression pin; (5) never-rejects with a bogus next_role, write still succeeds and persists (verified via storage.parse); (6) E28 coexistence — one write, two warnings, neither clobbered. Regression pins independently verified: reverted round-1's fix (hardcoded feature_changed:false) in compiled dist/ -> S-E37-hi fails as expected; reverted round-2's fix (agent-only self-loop filter) -> R-QA-IP and R-PM-BLOCKED fail as expected; dist restored byte-identical (diff clean) and rebuilt via tsc afterward. Gates: npm run build zero errors; npm test 1633/1633 (baseline 1623 + 10 new); npm audit --audit-level=high unchanged (4 pre-existing HIGH transitives: esbuild, fast-uri, js-yaml, sharp — no dependency file in scope, non-blocking per standing item). tools/transitions.ts and tools/handoff-orchestrator.ts untouched by this QA round (test-only diff). No fix for E42 fell out of this work — the empty-set fallback's deadness (F1 in the review) is a cross-module invariant (computeNewRound resets round counters on pm:In_Progress) that these tests don't need to pin directly, since the round-cap whitelist test already exercises pm:In_Progress with a round cap armed without hitting the fallback; leaving E42 open as filed.

