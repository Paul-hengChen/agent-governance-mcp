# QA Review — T-E50-03

covers: T-E50-03, T-E50-01, T-E50-02

Feature: `e50-release-sop-step7a-hardening` · qa-engineer, round 1 (code-reviewer APPROVED at review_round 2; content-only mini-chain, backlog E50 row IS the spec)

## Phase 0.5 — Expected-Red Diff
Skipped (no `qa_reports/expected-red_e50-release-sop-step7a-hardening.txt` declared; content-only ticket, not bugfix mode).

## Phase 1 — Review

Read `review_reports/review_T-E50-02.md` in full — both rounds — before writing a single test. Adopting its verified conclusions rather than re-deriving them: F8 (unbound `$CODES`) and F9 (global-OR guard, permanent wedge) CLOSED at round 2, verified by extracting the shipped fences verbatim with `awk` and executing those bytes in seven detached worktrees plus five scratch repos; the E49 membership predicate and step 8/AC4 (E44) both byte-identical to `HEAD`; F10 honored (Blocked row removed, halt-and-surface inline); N11/N12/N13 resolved. `git status --short` confirms the boundary independently: this session's own diff is `test/release-staging.test.mjs` only, plus governance bookkeeping (`.current/handoff.md`, `.current/telemetry.jsonl`, `tasks.md`); `content/skill-release-engineer.md` is untouched by this ticket (review-approved, out of QA's edit boundary per the dispatch brief).

### 3a/3b Copy & Visual Audit Gate
N/A — no `specs/e50*.md` / `design/e50*.md` (backlog row = spec, per `scope_decision_why`), so there is no Copy/Strings or Visual Tokens H2 to audit against. The change is SOP prose in `content/skill-release-engineer.md`, already audited exhaustively by code-reviewer over 2 rounds (F8/F9/F10 above), and QA's own remit here is test coverage, not a third correctness pass.

## Phase 1.5 — Visual Compare
Skipped (no `design/<feature>.md`, no `## Visual Baselines` H2).

## Test-coverage note disposition (review_T-E50-02.md's 12-item list, ordered by consequence)

All 12 items covered. `test/release-staging.test.mjs`'s E49/E50 block (Phase 7/8) was substantially rewritten, not merely appended to — the pre-existing helper `deriveCodesFromWorkingTree` now shares its per-file extraction with a new `codeFromFilename` (the shipped `sed`/`grep -oE`/`tr` chain, anchored `^[a-z_]*T-([A-Za-z0-9]+)-`), and three new model functions (`deriveGuardFlags`, `deriveCodesUnion`, `resolveDestination`/`resolveMoves`) mirror the shipped guard and union logic without spawning real git processes, consistent with this suite's existing behavioral-simulation convention (`test/feature-scope-gate.test.mjs`, `test/researcher-deep-research.test.mjs`).

- **Item 0 (the critical one — flip in place)**: DONE. The stale pin at the old `:1087-1106` location (`assert.deepEqual(codes, ["E45","OLD"])` labeled "current (unguarded) behavior … NOT this ticket's to fix") is rewritten in place, not left standing beside a new test. It now asserts `deriveGuardFlags(...).STOP_QA === true` for the exact empty-baseline-plus-root-files shape, and carries `["E45","OLD"]` forward as a sanity check on what the (now-gated) derivation would still produce if reached — the set that must never surface in practice.
- **Item 1 (N14 — repoint the fence pin)**: DONE. The old fence-match regex (`` /```\n(\s*find qa_reports -maxdepth 1[\s\S]*?)```/ ``, which the reviewer proved matches the illustrative fence, not the executable one) is replaced with an anchor on `` CODES=$( { `` — the fence the role actually executes. A new supplementary test additionally pins that the illustrative and executable fences carry a byte-identical `find`/`grep -vxFf` pair (modulo the `EXCLUDE_*` guard clause and whitespace) per tree, so the known duplication cannot silently diverge before a follow-up ticket collapses it.
- **Item 2 (`CODES` bound, not free)**: DONE via the repointed Item-1 test (asserts `CODES=` presence and no `git log`/`--diff-filter=A` in the executable fence) plus the pre-existing "skill text pins the ACTUALLY SHIPPED derivation" test (unchanged, still green) plus behavioral coverage in the seven-release backtest (below) which exercises the v3.94.0 `{E37 E38}` shape through the model.
- **Item 3 (14-row filename-shape matrix)**: DONE. All 14 rows from the review's table are pinned against `codeFromFilename`, table-driven — `RELSOP`, `E4X`, `E36`, `E11E12`, `C7`, bare `T-E51-01.md`→`E51`, both `expected-red_*` shapes → no code, both legacy pre-convention names → no code, `README.md`/`.DS_Store`-class → no code, `REVIEW_T-...`/`review_t-...` case mismatches → no code, and `review_T-PGAT.md` (N16) → no code. A second test separately pins N15's two documented over-accepts (`notes_about_T-E99-01_backup.md`→`E99`, `archive_T-E51-01.md`→`E51`) as a recorded-not-fixed hazard, not a passing grade.
- **Item 4 (repeated-release shape + directory absence)**: DONE, two tests. The permanent-wedge fixture from round 1 is re-run across THREE consecutive releases in a workspace whose `review_reports/` tree never exists — a fix that merely deferred the STOP (e.g. cleared only at release 2) would fail this test, since all three releases are asserted, not just the second. A separate test pins that an absent `review_reports/` tree resolves to `EXCLUDE_RR` (not an error) and contributes nothing to `<CODES>` even if a caller mistakenly hands it working-tree data.
- **Item 5/7 (union adds no codes across the seven-release fixture set)**: DONE. The reviewer's round-2 seven-release table (`v3.91.0`…`v3.96.0`) is ported into fixtures and the union is asserted equal to the `qa_reports` column on every row, with `v3.96.0` — the only release where `review_reports/` contributes anything — called out explicitly.
- **Item 6 (parallel destination, no cross-filing)**: DONE. The real `review_T-E4X-03.md` collision (present in both trees at `v3.96.0`) is the fixture; asserts both files are scheduled to move and the two destinations differ, closing exactly the `mv -n` silent-drop class the parallel-archive-dir pin exists to prevent.
- **Item 7 (code in one tree only)**: DONE, using the real `v3.96.0` shape — `E44`/`E49` enter `<CODES>` from `qa_reports/` alone; the `review_reports/` move matches nothing for those two codes and only moves the genuinely-shared `E4X` file.
- **Item 8 (`covers:` sweep stays in its own tree)**: DONE — asserts destination resolution is keyed to source tree, never to which codes matched, so a `review_reports/*.md` file's `covers:` sweep can never resolve to `qa_reports/archive/`.
- **Item 9 (non-retroactivity — highest-consequence regression pin)**: DONE. Extended to `review_reports/`: a baseline containing a root file excludes that id (using the real `E45`-in-`v3.95.0`-baseline shape), plus a scaled check that a legacy `review_reports/` file already in every recent baseline is never swept regardless of age — the "103 legacy files" class named in the brief.
- **Item 10 (E44/E49 non-regression)**: DONE by construction — every pre-existing E49 test (F7 v3.93.0, F7 v3.94.0, committed-in-range, non-retroactivity-qa, F4 bare-code, already-archived, N2 expected-red, the "skill text pins ACTUALLY SHIPPED derivation" test, and the step-order pin) is left byte-for-byte unmodified. Confirmed green in the full run below.
- **Guard ordering / no-writes (new, not itemized but explicitly requested in the brief)**: DONE — one new test asserts the guard block precedes both the `CODES=` derivation and the `mkdir` bullet in file order, and that the guard's own fenced code contains no `mv`/`mkdir`/`git add`.

**Not fixed, and correctly so per the brief's boundary**: N15 (over-wide `[a-z_]*` class), N16 (no-trailing-segment ids invisible end-to-end, pre-existing and internally consistent), N17 (`covers:` sweep not `EXCLUDE_*`-conditioned, cosmetic), N18 (unset `PREV_TAG` STOPs both trees, explicitly Round-1-endorsed behavior) are all recorded as pins or explicit non-fixes above, never worked around. None of the four warrant a blocking verdict on inspection: N15/N17/N18 are cosmetic or already-endorsed, and N16 is a pre-existing, internally-consistent gap (derivation and moves agree) that predates E50 and is out of this ticket's scope. Fixing `content/skill-release-engineer.md` for any of them would also cross the stated boundary (review-approved file, not QA's to edit).

## Phase 3 — Tests

Test File Discovery: `test/release-staging.test.mjs` already exists and is the file the coverage note names throughout — no new test file warranted.

Spec-to-Test Map (backlog E50 row is the spec; mapping is to the row's four fixes (a)-(d) plus the review's F8/F9/F10 findings, since there is no `specs/e50*.md` with `proof:`-annotated ACs):

| Spec item | Test(s) |
|---|---|
| (a) empty-baseline guard, per-tree (F9) | "F9 — per-tree guard, all shapes"; Item 0 flip test; Item 4 repeated-release + directory-absence tests |
| (b) zero-match logging, `CODES` bound (F8) | Item 1/N14 fence-repoint test; "skill text pins ACTUALLY SHIPPED derivation" (pre-existing, unmodified) |
| (c) `review_reports/` scan scope, parallel archive dir | Item 5/7 seven-release backtest; Item 6 parallel-destination test; Item 7 code-in-one-tree test; Item 8 covers-sweep test; Item 9 non-retroactivity test |
| (d) N5 bash/zsh parenthetical | No dedicated test (prose-only, non-behavioral; consistent with this file's own precedent of not test-covering parenthetical rationale) |
| F10 (Blocked-row removal, halt-and-surface) | Not re-tested here — F10's resolution is SOP-prose-only (no `:Blocked` row to grep for); `git diff --stat` confirms `tools/transitions.ts` untouched, matching the reviewer's own verification method |

Coverage Gate: 100% of the 12-item coverage note is addressed (line-count not meaningful for a pure test-model file; every new behavioral surface introduced by E50 — guard flags, union derivation, extraction chain, destination resolution — has at least one dedicated assertion, several have both a positive and an adversarial fixture).

Security Smoke Tests: N/A in the traditional sense (no auth/permission surface; this is release-tooling SOP prose). The adversarial filename shapes in Item 3 (`expected-red_T-E51-01.txt`, uppercase/lowercase-swapped prefixes, no-trailing-segment ids) function as this ticket's boundary-input coverage — each is a filename crafted to look like it should produce a code but must not, or vice versa.

## Phase 3.5 — AC Execution Log
Skipped (no `specs/e50*.md`, so no `proof:`-annotated ACs to execute).

## Phase 4 — Run

- `npm run build` — clean, zero errors. `check:version — dist/index.js parity OK (3.96.0)`; `check:version — OK (3.96.0)` (HEAD past the v3.96.0 tag, unrelated to this ticket).
- `node --test test/release-staging.test.mjs` — **53/53 passing** in isolation (41 pre-existing + 12 net new: Item-0 flip and the N14 fence-repoint each REPLACE a prior test in place, so the file gained exactly 12 tests, not 14).
- `npm test` (full suite) — **1669/1669 passing** (0 fail), up from the pre-ticket baseline of **1657/1657** — the +12 delta matches the release-staging file's own net gain exactly; no other file's test count moved.
- `npm audit --audit-level=high` — **11 pre-existing advisories** (js-yaml, protobufjs, sharp/@xenova-transformers — 2 low, 4 moderate, 5 high), unchanged from baseline; `package.json`/`package-lock.json` untouched (`git status --short` shows no changes to either).
- Scope re-confirmed via `git status --short`: `test/release-staging.test.mjs` (this session's only source-tree edit) plus governance state (`.current/handoff.md`, `.current/telemetry.jsonl`, `tasks.md`); `content/skill-release-engineer.md` is unmodified by this session — confirmed untouched, consistent with the QA boundary (review-approved, sr-engineer's file, not QA's to edit).

## Verdict

**PASS.** All 12 items from `review_reports/review_T-E50-02.md`'s "Test-coverage note for T-E50-03 — FINAL" are addressed, with the two highest-consequence items handled exactly as instructed: Item 0's stale permissive pin is rewritten in place (not left standing beside a competing test) to assert the new per-tree STOP instead of sanctioning the mass-sweep hazard E50 exists to close, and Item 1's fence pin is repointed at the executable `CODES=` block so the suite can no longer go green on a reintroduced `git log`/`--diff-filter=A` regression in the block the role actually runs. The 14-row filename-shape matrix, the repeated-release and directory-absence shapes, the seven-release union backtest, the parallel-destination collision, and both non-retroactivity pins (qa_reports/ pre-existing, review_reports/ newly extended) are all pinned against models built directly from the shipped SOP text, not from either discarded draft. N15/N16/N17/N18 are recorded, not worked around, and none rise to blocking on inspection. Build clean, full suite green (1669/1669, +12 over baseline), audit unchanged at 11 pre-existing advisories. No edits to `content/skill-release-engineer.md`, the constitution, or any file outside this ticket's test-and-evidence boundary.
## 2026-08-11T03:23:16.088Z — PASS — by qa-engineer

PASS. Read review_reports/review_T-E50-02.md in full (both rounds, APPROVED round 2 — F8/F9 CLOSED, F10 honored) before writing tests. Rewrote test/release-staging.test.mjs's E49/E50 block: added codeFromFilename (shipped sed/grep -oE/tr chain, anchored ^[a-z_]*T-([A-Za-z0-9]+)-), deriveGuardFlags (per-tree STOP_QA/STOP_RR/EXCLUDE_QA/EXCLUDE_RR), deriveCodesUnion, and resolveDestination/resolveMoves models. All 12 items from the review's coverage note addressed: Item 0 — the stale permissive empty-baseline pin (asserting ["E45","OLD"] as specified/unguarded behavior) rewritten IN PLACE to assert the new per-tree STOP fires instead, carrying the fixture forward as the set that must never surface; Item 1 (N14) — fence pin repointed from the illustrative find/grep fence to the executable CODES=$( { ... } ) fence, plus a new fence-identity test pinning the two fences' find/grep -vxFf pairs stay byte-identical until a follow-up collapses them; Items 2-9 all covered with dedicated tests (CODES binding, 14-row filename-shape matrix incl. both expected-red_* adversarial cases, per-tree guard combinations, the three-consecutive-release wedge fixture, directory-absence, the seven-release union backtest ported from the review's own table, the real v3.96.0 basename-collision parallel-destination test, code-in-one-tree-only, covers: sweep tree-fidelity, and non-retroactivity extended to review_reports/ including a legacy-file scale check); Item 10 (E44/E49 non-regression) preserved by leaving every pre-existing E49 test byte-unmodified. N15/N16/N17/N18 recorded as pins/notes, not worked around — none blocking on inspection (N15/N17/N18 cosmetic or already-endorsed, N16 pre-existing and internally consistent). Did not edit content/skill-release-engineer.md (review-approved, outside QA's boundary) or any other source/constitution file — git status --short confirms test/release-staging.test.mjs as the only source-tree change this session. Gates: npm run build clean (0 errors, check:version OK 3.96.0); node --test test/release-staging.test.mjs 53/53 (41 pre-existing + 12 net new, two items rewritten in place); npm test full suite 1669/1669 passing (0 fail), up from the 1657/1657 baseline by exactly +12; npm audit --audit-level=high unchanged at 11 pre-existing advisories (js-yaml, protobufjs, sharp/@xenova-transformers). Full evidence: qa_reports/review_T-E50-03.md (covers: T-E50-03, T-E50-01, T-E50-02).

