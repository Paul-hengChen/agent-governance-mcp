# QA Review — T-E46-01

Feature: `e46-qa-spec-defect-status-rule` · qa-engineer, round 1 (code-reviewer APPROVED at review_round 3; no QA rounds needed — no issues found in Phase 1)

## Phase 0.5 — Expected-Red Diff
Skipped (no `qa_reports/expected-red_e46-qa-spec-defect-status-rule.txt` declared; content-only ticket, not bugfix mode).

## Phase 1 — Review

Read `review_reports/review_T-E46-01.md` in full (all 3 rounds) before re-deriving anything. Adopting its verified conclusions: F1–F4 closed, scope clean (`content/skill-qa-engineer.md`, `content/skill-qa-visual.md` + governance state only), `.current/telemetry.jsonl`'s appended line is a server-rejected `TRANSITION_REJECTED` from code-reviewer's round-2 write attempt, not a source edit.

### 3a/3b Copy & Visual Audit Gate
N/A — this ticket has no `specs/e46*.md` / `design/e46*.md` (backlog row = spec, per `scope_decision_why`), so there is no Copy/Strings or Visual Tokens H2 to audit against. The change is SOP prose in `content/skill-qa-engineer.md` / `content/skill-qa-visual.md` themselves, already audited exhaustively by code-reviewer over 3 rounds (see F1–F4 above).

## Phase 1.5 — Visual Compare
Skipped (no `design/<feature>.md`, no `## Visual Baselines` H2).

## SOP-consumer judgment (the part QA owns, not code-reviewer)

**1. Reachability at the decision point, read in order, no look-ahead.** Read `content/skill-qa-engineer.md` top to bottom as a round-working agent would: Phase 3a Drift (`:44`) and 3b Drift (`:50`) each end "First check § *Contract Defect vs Implementation Failure* — if a sanctioned divergence supersedes the spec's text/literal, this is `Blocked`, not `FAIL`." Both name the section literally (it exists verbatim as `## Contract Defect vs Implementation Failure` at `:89`). Landing there without prior context, `:91`–`:99` is self-contained: the two-verdict test (`:94`–`:95`), the WHEN/DO/ELSE route (`:97`) with its own citation clause, and the coverage-gap carve-out (`:99`) all resolve without needing to have read ahead to the Escalation Routes table — the row it points to (`:108`, "contract defect | Blocked | ... | pm") is consistent with what `:97` already states inline (`blocking_reason` format, `next_role: pm`). Confirmed: the routing decision is makeable at the decision point, matching round 3's own conclusion, independently re-derived rather than taken on faith.

**2. `Blocked` is not reachable on mere disagreement.** `:97`'s guard requires `blocking_reason` to cite an artifact that is (a) not QA-authored and (b) predates the QA round — "the PM/coordinator attestation on the cut-approving `pm:In_Progress` write, or a PM-authored divergence table in `specs/<feature>.md`." A `qa_reports/visual_<id>.md` `## Allowed Differences` entry is explicitly named and disqualified in the same sentence ("does NOT satisfy this... lets QA self-certify the very escape this guard exists to gate"). No-citation path fails safe: "No qualifying artifact on record → the claim is an implementation failure wearing a cheaper label; FAIL it." Traced against `cut_approved`/`scope_decision_why` on this very feature's handoff state (both PM-authored, predate this QA round, non-QA fields) — a real, checkable citation target exists and is not something QA itself could have written. Independently re-confirmed round 3's escape-table (`:189`–`:196` of the review report): every self-certification path is closed by name or by category, and the one residual (staleness-read-as-sanction) fails toward `FAIL`, the safe direction.

**3. Test coverage — new row is covered, new section's prose is not, and that is consistent with repo convention.** `test/ac-execution.test.mjs:453` pins the Escalation Routes table's exact row count (now 7): removing the `contract defect` row (or any other) trips this test, so the row is covered, not merely tolerated. The `## Contract Defect vs Implementation Failure` H2's actual prose (the two-verdict test, the WHEN/DO/ELSE route, the anti-abuse citation clause) has no dedicated content-assertion test — grepped `test/` for "sanctioned divergence", "Decision test", "WHEN both verdicts", "Contract Defect vs Implementation" and found no hits. I checked whether this is a gap or the established pattern: it's the pattern. `gates/ac-execution.ts` has a server-side `DISPOSITION_HEADING` literal-match dependency on `## AC Execution Log`, which is why that heading (and `**Phase 3.5 — AC Execution**`) gets its own `body.includes(...)` assertions (`test/ac-execution.test.mjs:421,430`). The Contract Defect section has no server-side gate keyed to its heading text or prose — it is a human/agent-facing routing convention, same shape as the Phase 0.5/3.5 "absent branch" prose and the Hard Rules bullets, none of which carry content-assertion tests beyond the aggregate byte cap. Given that, byte-cap + row-count is proportionate, not under-covered relative to this file's own precedent. Did not add a new test beyond the two handed edits.

## Phase 3 — Tests

Test File Discovery: the two relevant test files (`test/qa-visual-skill-split.test.mjs`, `test/ac-execution.test.mjs`) already exist and were the ones needing edits — no new test file warranted (see judgment #3 above).

- `test/qa-visual-skill-split.test.mjs:167` — cap raised `15500 -> 17900` with a dated (2026-08-10) comment naming E46, matching the file's own convention (measured size 17512, ~388-byte headroom, tighter end of the ~350–550 convention per round 3's explicit rejection of 18000). `content/skill-qa-visual.md` cap (`:197`, 20700) untouched — file is 17007, no bump needed.
- `test/ac-execution.test.mjs:453` — re-scoped, not renumbered: `rows.length` assertion moved 6 -> 7, comment rewritten to state AC6 itself still adds no row (Phase 3.5 reuses the pre-existing Phase 4 FAIL route) and attribute the 7th row to E46's unrelated contract-defect addition. The enclosing test name (`:435`, "introduces NO new escalation-table row") stays true of AC6.

## Phase 4 — Run

- `npm run build` — clean, zero errors. `check:version` OK (3.94.0; HEAD past the v3.94.0 tag, unrelated to this ticket).
- `node --test test/*.test.mjs` — **1641/1641 passing** (0 fail), matching the pre-ticket baseline exactly. The two failures this ticket was handed (`test/ac-execution.test.mjs:453`, `test/qa-visual-skill-split.test.mjs:167`) are both green.
- `npm audit --audit-level=high` — 11 pre-existing advisories (ip-address, js-yaml, protobufjs, sharp/@xenova-transformers), `package.json`/`package-lock.json` untouched (`git status --short` shows no changes to either). Recorded as pre-existing per the ticket's instructions; not fixed.
- Scope re-confirmed via `git status --short`: `content/skill-qa-engineer.md`, `content/skill-qa-visual.md`, `test/ac-execution.test.mjs`, `test/qa-visual-skill-split.test.mjs`, plus governance state (`.current/handoff.md`, `.current/telemetry.jsonl`, `tasks.md`) and the untracked `review_reports/review_T-E46-01.md`. No other file moved.

## Verdict

**PASS.** The rule is reachable at the decision point without look-ahead, structurally closed against mere spec disagreement (fails safe to `FAIL`), the new escalation row is test-covered (not merely tolerated) and the new section's prose is uncovered only in the same way every other non-server-gated SOP-prose addition in this file is uncovered — consistent with, not short of, established convention. Both handed test edits verified correct against the reviewer's reasoning, not just applied mechanically. Build clean, full suite green (1641/1641), audit pre-existing and unchanged.
## 2026-08-10T06:38:01.523Z — PASS — by qa-engineer

PASS. Read review_reports/review_T-E46-01.md in full (3 rounds, APPROVED at round 3) before re-deriving. Applied both handed test edits per the reviewer's reasoning, not just the instruction: test/qa-visual-skill-split.test.mjs:167 cap 15500->17900 (measured 17512, 388-byte headroom, tighter end of convention per round 3's explicit rejection of 18000 — dated comment naming E46 added); skill-qa-visual.md needed no bump (17007, unchanged). test/ac-execution.test.mjs:453 re-scoped (6->7), not renumbered — AC6's own claim ('introduces NO new escalation-table row') stays true, comment now attributes the 7th row to E46's unrelated contract-defect addition. As the SOP's consumer: read the new '## Contract Defect vs Implementation Failure' section and the two Phase 3a/3b Drift bullets (:44, :50) in order, as a round-working agent would — the routing decision (FAIL vs Blocked) is reachable at the decision point without having read ahead, since both Drift bullets name the section literally and the section is self-contained. Confirmed the Blocked route cannot be reached on mere spec disagreement: the anti-abuse guard at :97 requires a citation to an artifact that is (a) not QA-authored and (b) predates the QA round (cut_approved/scope_decision_why on the pm:In_Progress write, or a PM-authored divergence table in specs/<feature>.md) — a qa_reports/visual_<id>.md Allowed Differences entry is explicitly disqualified in the same sentence, and no-citation fails safe toward FAIL. Test coverage: the new Escalation Routes row is covered (not merely tolerated) by ac-execution.test.mjs's exact row-count pin; the new section's prose has no dedicated content-assertion test, but this matches established repo convention — only sections with a server-side gate keyed to their heading (e.g. AC Execution Log) get literal-match tests, and Contract Defect has no such gate. Did not add new tests beyond the two handed edits. Gates: npm run build clean; node --test test/*.test.mjs = 1641/1641 passing (matches pre-ticket baseline exactly, both handed-in failures now green); npm audit --audit-level=high shows the same 11 pre-existing advisories, package.json/package-lock.json untouched — recorded as pre-existing, not fixed. .current/telemetry.jsonl's appended line is code-reviewer's server-rejected TRANSITION_REJECTED from round 2, not a source edit. Full review: qa_reports/review_T-E46-01.md.

