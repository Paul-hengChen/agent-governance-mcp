# QA review — T-E52-01

<!-- Auto-appended by tw_update_state(qa_review=...). -->

## 2026-08-20T10:20:08.690Z — FAIL — by qa-engineer

T-E52-01 verified independently against git diff ground truth, not the brief. VERDICT: FAIL — one defect class, three instances, all in docs/backlog.md.

CLEAN (verified independently):
(1) Semantics claims in tools/metrics.ts, docs/gate-retro-procedure.md, scripts/summarize-metrics.mjs are ALL TRUE of the code. Read tools/transitions.ts computeNewRound (lines 601-687) myself: qa_rounds_total/review_rounds_total/visual_rounds_total increment ONLY on (qa-engineer,FAIL), (code-reviewer,FAIL), (qa-engineer,FAIL+visual_fail token) respectively — a round ending APPROVED/PASS increments nothing, matching the new "rework only" prose exactly. specs/e8-success-telemetry.md AC3 (lines 40-46) confirms this is spec-defined, using the exact "code-reviewer CHANGES_REQUESTED" phrasing the new prose borrows. one_pass definition claim verified against tools/metrics.ts:131-132 (current) — matches. docs/gate-retro-procedure.md:83 pre-cut text independently confirmed to already say "rework rounds" (git show HEAD), so the "distorted retro" premise-kill claim is true. Zero gate reads *_rounds_total (grep gates/*.ts: no hits) — confirmed, so the "dissolves the ROUND_CAP question" claim is true.
(2) Zero behavioural change: npm run build clean; full suite 1745/1745 green (re-run independently, not trusted from the brief); npm audit --audit-level=high: 5 findings, all low/moderate, zero HIGH/CRITICAL. dist/tools/metrics.js diff is comment-only (2 lines), confirms source change didn't alter emitted JS logic. test/success-metrics.test.mjs run standalone: 36/36 green including E8-S1..S4 by name.
(3) docs/backlog.md table integrity: new `| E85 |` row = 7 pipes (header `| id | desc | priority | depends_on | est. files | design-link |` = 7 pipes); new `| 10a |` row = 5 pipes (header `| order | ticket | intake | why here |` = 5 pipes); edited `| E52 |` row = 7 pipes; edited `| 10 | **E52**` row = 5 pipes. All match, and a backtick-span sweep of both new rows found no unescaped `|` hiding inside code spans (the E74 false-positive trap) — cell counts are genuinely correct, not accidentally correct.
(4) .current/metrics.jsonl: git diff empty, byte-identical to HEAD. No backfill.
(5) Scope containment: git status shows only tools/metrics.ts, docs/gate-retro-procedure.md, scripts/summarize-metrics.mjs, docs/backlog.md, tasks.md, .current/handoff.md, and dist/tools/metrics.{js,js.map,d.ts.map} (expected build artifact of the metrics.ts comment change). Nothing else touched.

FAIL — docs/backlog.md's new E52 DONE paragraph makes three specific, checkable code-location claims and gets them wrong:
  a. "`tools/metrics.ts:114-115`" for the one_pass computation — that WAS correct pre-cut (git show HEAD confirms 114-115) but the cut's OWN sibling edit to the same file (the new ROUND SEMANTICS comment block) pushed it to lines 131-132. Self-inflicted staleness: the row documents this cut's semantics fix but its own citation was never re-checked after the accompanying edit.
  b. "`tools/metrics.ts:40-47`" for the emitFeatureMetrics signature — same story, was correct pre-cut, now at lines 55-62 post-cut.
  c. "`tools/handoff-orchestrator.ts:1543`" claimed as "sole call site" of emitFeatureMetrics — this is not stale, it is simply wrong: handoff-orchestrator.ts was never touched by this cut (confirmed via git status), and line 1543 in the CURRENT (and pre-cut) file sits inside the unrelated storage.writeState({...}) call (the `qaRoundsTotal: new_qa_rounds_total,` field, line 1542). The actual, only emitFeatureMetrics call site is at line 1622.

These don't change the substance of the conclusion (DO-NOT-BUILD (i)/(ii), rework-only semantics, no gate reads totals — all independently reconfirmed true above), but this ticket exists specifically because prose about these fields has misled five readers already, and a governance-durable backlog row citing the wrong line for its own key facts is the same failure mode at smaller scale. Trivial fix: update the three citations to tools/metrics.ts:131-132, tools/metrics.ts:55-62, and tools/handoff-orchestrator.ts:1622 (or drop exact line numbers if they're expected to drift). No other changes needed — this is a documentation-only re-open, not a rebuild.


## 2026-08-20T10:29:32.000Z — Round 2 — PASS — by qa-engineer

Scope per brief: docs/backlog.md ONLY. Verified via mtime that tools/metrics.ts, docs/gate-retro-procedure.md, scripts/summarize-metrics.mjs are all last-touched 18:08:38 — before docs/backlog.md's 18:21:22 edit — and not re-audited from scratch, per instruction.

**1. Symbol anchors resolve and are true** (opened all three files, line-checked each):
  - "the `one_pass:` field of the record literal built in `emitFeatureMetrics`" → tools/metrics.ts:121-133, `const record: FeatureMetricRecord = { ... one_pass: args.qaRoundsTotal === 0 && ... }`. Real, unambiguous. TRUE.
  - "its `args` signature in `tools/metrics.ts`" (emitFeatureMetrics receives only the three totals, no verdict) → tools/metrics.ts:55-62, `export function emitFeatureMetrics(args: { workspacePath, feature, qaRoundsTotal, reviewRoundsTotal, visualRoundsTotal, hops })`. No verdict field present. TRUE.
  - "the cumulative-totals block of `computeNewRound`" → tools/transitions.ts:665-690, explicitly comment-labeled "v12 cumulative totals (e8-success-telemetry)"; `review_rounds_total` increments only on `next.agent === "code-reviewer" && next.status === "FAIL"`. Real, named block, not vague. TRUE.
  - "the release-close terminal-marker branch in `tools/handoff-orchestrator.ts`" as sole call site → grep confirms exactly ONE `emitFeatureMetrics(` call in the file, at line 1622, gated on `agent_id === "release-engineer" && status === "In_Progress" && next_role === "pm"`, directly preceded by a comment naming it "the E1A terminal-marker signature." TRUE and sole.
  - Bonus cross-check not required by the brief but load-bearing for anchor 1: `specs/e8-success-telemetry-architecture.md:56` defines `one_pass` identically (`qa_rounds===0 && review_rounds===0 && visual_rounds===0 (AC3)`), confirming the "FeatureMetricRecord sketch says the same" clause. TRUE.
  None of the anchors are vague-unto-unfalsifiable — each names a specific, greppable construct (a field, a function signature, a labeled block, a call site) that resolves to exactly one place in the tree.

**2. No live line-number citation survives**: `grep -c "tools/metrics.ts:114-115"` → 1 hit total in the whole file, and it sits inside the new history sentence ("...the DONE paragraph above originally cited `tools/metrics.ts:114-115` and `:40-47` — both moved... Fixed by making all three references symbol-anchored..."). Same sentence also carries the sole `tools/handoff-orchestrator.ts:1543` hit in the file. Confirmed by direct string context (python slice), not just grep count. Independently reconfirmed the history's claim about :1543 itself: handoff-orchestrator.ts:1543 is `reviewRoundsTotal: new_review_rounds_total,` inside an unrelated storage-write object literal (line 1537 comment: "v12 — cumulative totals... persisted in file-mode frontmatter") — genuinely unrelated to emitFeatureMetrics, as the history sentence says. `docs/gate-retro-procedure.md:83` (cited in my round-1 verdict, not part of this cut's own citations) still resolves: line 83 reads "The round columns are **rework counts, not rounds-run**" — untouched, correct.

**3. Table integrity** (E74 class), verified with a script that also strips backtick code-spans to rule out hidden pipes: `| E52 |` row = 7 pipes = header (`| id | desc | priority | depends_on | est. files | design-link |` = 7 pipes), non-code-pipe count also 7 (no pipes hidden in code spans). `| E85 |` row unchanged, 7 pipes, matches. `| 10 | **E52**` row = 5 pipes = header (`| order | ticket | intake | why here |` = 5 pipes). `| 10a | **E85**` row unchanged, 5 pipes, matches. All four rows: raw pipe count === non-code pipe count, so the match is genuine, not an E74 false-positive.

**4. Scope containment**: `git status --short` shows only `.current/handoff.md`, `dist/tools/metrics.{js,js.map,d.ts.map}`, `docs/backlog.md`, `docs/gate-retro-procedure.md`, `scripts/summarize-metrics.mjs`, `tasks.md`, `tools/metrics.ts` modified, plus untracked `qa_reports/review_T-E52-01.md`. `dist/tools/metrics.js` diff re-checked: still exactly the 2-comment-line addition from round 1 (byte-diffed), no new logic despite the later mtime (18:21:39, a rebuild that didn't change output). `tasks.md`'s only diff is the still-unchecked `T-E52-01` line added in round 1 — benign. `.current/metrics.jsonl`: `git diff --quiet HEAD` → identical, confirmed.

**5. Real numbers, independently re-run**:
  - `npm run build`: clean (check:version OK 3.103.0, tsc clean, check:transitions-sync OK 21 keys).
  - `npm test`: **1745/1745 pass**, 0 fail, 57.6s.
  - `npm audit --audit-level=high`: **5 vulnerabilities (2 low, 3 moderate) — zero HIGH/CRITICAL** (hono, body-parser, esbuild, protobufjs, @hono/node-server transitive deps; fixes available via `npm audit fix` but none are high/critical).

**Verdict: PASS.** The symbol-anchor fix is a genuine improvement, not a cosmetic dodge — every anchor names a real, checkable construct and the round-1 stale/wrong line numbers survive only as quoted, clearly-labeled history. No scope creep, no table damage, no behavioural change, build/test/audit all clean.

**Note for the record, NOT part of this task's pass/fail and NOT filed by QA**: the handoff's `scope_decision_why` field previously picked up literal tool-call tag fragments on two earlier coordinator writes (malformed tool calls, not content). Grepped the current `.current/handoff.md` for stray tags (`antml`, `function_calls`, `invoke`, `parameter name`) — zero hits, current copy is clean, substance was never altered. This is a governance-record-integrity defect worth a backlog row (E17-adjacent: coordinator write hygiene / tool-call leakage into attestation fields) — recommend PM file it, but it does not affect T-E52-01.
## 2026-08-20T10:30:14.690Z — PASS — by qa-engineer

Round 2 PASS. Scope was docs/backlog.md only; the other 3 cut files confirmed untouched since round 1 via mtime (18:08:38, before backlog.md's 18:21:22 edit). All 4 symbol anchors in the E52 cell (one_pass field in emitFeatureMetrics' record literal, emitFeatureMetrics' args signature, computeNewRound's cumulative-totals block, the release-close terminal-marker branch as sole call site) independently opened and confirmed true and non-vague against tools/metrics.ts, tools/transitions.ts, tools/handoff-orchestrator.ts. The only surviving tools/metrics.ts:114-115 / :40-47 / handoff-orchestrator.ts:1543 citations sit inside the new history sentence describing them as the wrong round-1 citations. Table integrity holds (E52/E85/10/10a cell counts match headers, no hidden pipes in code spans). Scope containment confirmed via git status + dist diff re-check (still comment-only) + metrics.jsonl byte-identical to HEAD. npm run build clean; npm test 1745/1745; npm audit --audit-level=high: 5 low/moderate, zero HIGH/CRITICAL. Full detail in qa_reports/review_T-E52-01.md Round 2 section. Separately flagged (not part of this task): scope_decision_why's earlier tool-call-tag contamination is confirmed clean in the current copy; recommend PM file an E17-adjacent governance-record-integrity backlog row.

