# QA review — T-QAVBP-03

<!-- Auto-appended by tw_update_state(qa_review=...). -->

## 2026-06-16T11:27:58.971Z — FAIL — by qa-engineer

T-QAVBP-04: test file authored (test/evidence-provenance.test.mjs, 34 tests). 667/668 pass. 1 sentinel test fails — exposing a real parser defect in tools/evidence-file.ts. BASELINE_LINE_RE and DIFF_METRIC_LINE_RE use `.+?` as the capture group with a trailing `\s*$`. Because `\s` matches `\n`, the regex engine satisfies `(.+?)` lazily by consuming the newline via the trailing `\s*` and then capturing the NEXT line. Reproduction: body = 'baseline:   \n- diff-metric: B1 tool unavailable — LLM fallback\n' → parser captures 'B1 tool unavailable — LLM fallback' as fingerprint (not null). This silently bypasses AC-1: a B1-fallback surface with NO real baseline value would pass the gate because the diff-metric token is not in FINGERPRINT_PLACEHOLDERS. Fix required: change .+? to [^\n]+? in BOTH BASELINE_LINE_RE and DIFF_METRIC_LINE_RE in tools/evidence-file.ts (lines 593–594), then npm run build. Pre-existing 2 HIGH npm audit advisories (esbuild/tsx, @xenova/transformers) noted — NOT introduced by this feature, waiver stands.

## 2026-06-16T11:34:32.928Z — PASS — by qa-engineer

PASS — qa_round=1. Test suite: 668/668 green (0 fail, 0 skip), confirmed by independent npm run build (tsc clean) + npm test. Sentinel test #115 'D1 placeholder blacklist: whitespace-only value before next field' GREEN. Cross-line capture bypass CLOSED: tools/evidence-file.ts lines 593-594 — both BASELINE_LINE_RE and DIFF_METRIC_LINE_RE now use [^\S\n] (horizontal-only whitespace) everywhere \\s appeared, plus [^\n]+? capture group. Reasoning is sound: these are single-line label match regexes (im flags, one match per line); \\s absorbs \\n causing the im-flagged regex to skip across a blank trailing value and steal the next line's content as the fingerprint; [^\S\n] cannot cross newlines so 'baseline:  \\n' produces empty capture -> .trim() -> '' -> FINGERPRINT_PLACEHOLDERS hit -> fingerprint=null. headRe (line 607, gm, heading-only) correctly left untouched. Legitimate multi-word fingerprints (e.g. 'figma-node 4091:46836 hash a1b2c3'), backtick-wrapped, and bold-label variants all capture correctly (verified live against dist/ via node). test/evidence-provenance.test.mjs not modified by sr-engineer (git log on test file shows no sr-engineer commit). npm audit --audit-level=high: 2 HIGH pre-existing (esbuild/tsx dev-dep GHSA-gv7w-rqvm-qjhr; @xenova/transformers via onnxruntime-web chain) — standing waiver, not introduced by this change, not blocking. All 5 QAVBP tasks completed.

